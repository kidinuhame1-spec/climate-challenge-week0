# Scripts

This folder contains helper scripts used during development. The Streamlit dashboard reads local CSVs from the `data/` directory which is intentionally git-ignored.

Usage notes:

- The app entrypoint is `app/main.py`. Run locally with:

```bash
streamlit run app/main.py
```
# Scripts

This folder contains reusable helper scripts for setup and data workflows.
